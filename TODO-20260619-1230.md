# Riddle Diary — Project TODO ｜ 湯姆瑞斗的日記本 — 計畫 TODO

> Created / 建立：2026-06-19 12:30

## Summary (English)

A Chrome extension that reskins claude.ai as a handwritten "magic diary". This file is the
development log: what's done, known issues, the roadmap, and the design decisions behind it.

- **Status:** working (v0.3.0). Cover → diary → bridged Claude conversation, bookmark/history, themed fonts.
- **Privacy:** local-only, no servers, no tracking. Details in `README.md` and `PRIVACY.md`.
- **Known issue:** rare Chinese glyphs outside the bundled font fall back and can render in a Korean face;
  fix = bundle LXGW WenKai TC as a fallback (see Backlog).
- **Roadmap:** fallback font, ink "sink‑in" animation, sounds, ChatGPT/Gemini support, Google‑Doc theme.

中文細項如下（本檔為開發日誌，詳細條目以繁體中文記錄）。

---

## ✅ 已完成 Completed（截至 v0.3.0）

- [x] Chrome Extension 骨架（MV3、content script、popup 開關）
- [x] 羊皮紙日記覆蓋層
- [x] 字型定案並可靠載入：中＝辰宇落雁體、英＝Tangerine，**兩者皆本地打包**（`chrome-extension://`，零對外請求）；右上按鈕、說明、書籤皆統一字型
- [x] 墨水浮現動畫（落筆逐字淡入；回覆改為「寫完後一次浮現」）
- [x] 橋接真正的 Claude 對話（寫入輸入框 → 送出 → 輪詢讀回應）
- [x] 回覆擷取乾淨化：鎖定 `.font-claude-message`、濾掉 `Claude responded:`／按鈕／思考區塊、去整段重複
- [x] 湯姆·瑞斗人設（首則訊息前置角色指示）
- [x] 修正 claude.ai CSS 滲入輸入框（`!important` 蓋掉深色底）
- [x] 輸入體驗：Enter 一律清空、IME 組字中的 Enter 不誤送、busy 時排隊後自動送
- [x] `chrome.*` 全包 try/catch + `extValid()`，擴充重載後舊分頁不再噴錯
- [x] 啟動書封（點擊翻開動畫）
- [x] 書籤 → 左側歷史面板：讀側邊欄 `a[href^="/chat/"]`、去重複標題、單行顯示、可切換、可開新對話
- [x] 點歷史 → 載入該對話既有訊息（user＝`[data-testid="user-message"]`、assistant＝`.font-claude-message`）鋪進日記，而非顯示開場白

### 🐞 已知小問題 Known issues
- 罕用字（超出辰宇落雁體 jf7000 字集）會逐字 fallback，macOS 偶爾挑到韓文字型 → 該字看起來像韓文。根治：本地打包霞鶩文楷 TC 當後備（見 Backlog）。

---

## 🔜 待辦 To do（本次新增）

### 1. 風格化「啟動對話」按鈕 — ✅ 已實作 v0.2（啟動書封）
做法：載入時先顯示「闔著的日記書封」（`#rd-cover`，黑底金字 `T. M. Riddle`），點擊播放翻開動畫（`rotateY`）再進入日記。歷史面板內另有「✚ 翻開新的一頁」開新對話。從歷史切換進來時以 `sessionStorage` 旗標略過書封。
待打磨：書封皮革質感、翻頁音效、開新對話時自動帶人設開場白。

<details><summary>原始構想</summary>

讓使用者用日記風格「開啟一段新對話」的入口，而不是直接落在既有對話上。

- [ ] 設計一個符合羊皮紙/魔法日記風格的啟動畫面或按鈕（例如：闔著的日記書封 → 點擊「翻開」才進入對話）
- [ ] 點擊後：在 claude.ai 開新對話（找「New chat」按鈕或導向 `https://claude.ai/new`），再蓋上日記層
- [ ] 思考觸發點：是 popup 裡的按鈕？還是日記覆蓋層上的一顆「翻開新的一頁」？
- [ ] 啟動時自動帶入人設開場白（可選）
- [ ] 進場動畫：書封淡入 → 翻開 → 扉頁浮字

</details>

### 2. 書籤 / 左側歷史記錄（翻頁）— ✅ 已實作 v0.2
做法：書頁左緣加「書籤」緞帶（`#rd-bookmark`），點擊讓歷史面板（`#rd-history`）從左滑出；讀取 claude.ai 側邊欄的 `a[href^="/chat/"]` 當對話清單，點某筆即導向該對話並重新蓋上日記（略過書封）。
⚠️ **待對 live claude.ai 驗證**：`SELECTORS.historyItem` 是否抓得到對話清單（側邊欄收合時連結可能不在 DOM）；必要時改用展開側邊欄或其他選擇器。
待打磨：翻頁改 3D 捲頁動畫、目前對話高亮、長清單捲動。

<details><summary>原始構想</summary>

左緣加一條「書籤」，點了往左翻出歷史對話列表。

- [ ] 在書頁左緣加可點擊的「書籤」緞帶元件
- [ ] 點擊 → 往左翻頁動畫，露出歷史記錄面板
- [ ] 歷史來源：讀取 claude.ai 側邊欄的對話清單（需新增一組 `SELECTORS`：對話列表項、標題、連結）
- [ ] 點某筆歷史 → 切換到該對話並重新蓋上日記層
- [ ] 翻頁視覺：CSS 3D `rotateY` 或書頁捲起效果；維持羊皮紙質感
- [ ] 邊界情況：很多筆歷史時的捲動；目前對話的高亮

</details>

---

## 🗂️ Backlog 待辦清單（之前提過、尚未做）

- [ ] 墨水「沉入紙面再回字」電影級動畫（落筆 → 暈染下沉 → 日記浮現）
- [ ] **本地打包霞鶩文楷 TC 當辰宇落雁體的後備字型**，解決罕用字 fallback 變韓文的問題（檔案大，可 subset 縮小）
- [x] 本地打包字型（`web_accessible_resources` + `@font-face`）——辰宇落雁體已採用，實測可繞過 CSP
- [ ] 音效：羽毛筆刮紙、翻頁聲
- [ ] 擴充到 ChatGPT / Gemini（各自一組 `SELECTORS` + match 規則）
- [ ] 主題切換：Google Doc 偽裝 / 其他角色人設

---

## ⚖️ 字型授權查證 Font license verification（待辦 — 重要）

> 中文字型圈歷來有不少版權糾紛（盜用、改作授權不清、商用爭議），**正式發佈/散佈前務必逐一查證**目前用到的每個字型授權，並把授權檔一併附進專案。

| 字型 | 用途 | 初步了解 | 待查證 |
|------|------|----------|--------|
| 辰宇落雁體 Chenyuluoyan | 中文主字型 | OFL 1.1，標示可商用、可散佈、可改作 | [ ] 確認 2.0 併入的「jf 7000」字集本身授權相容；保留 `license.txt` |
| Tangerine | 英文 | Google Fonts | ✅ 已確認 **SIL OFL 1.1**（"All rights reserved" 為 OFL 版權人標準寫法，全文即 OFL；允許嵌入/隨軟體散佈） |
| 霞鶩文楷 LXGW WenKai TC | 中文補字 fallback | OFL 1.1 | [ ] 確認 TC 版本散佈條款 |
| IM Fell English | （已換掉，備選） | OFL | [ ] 若日後啟用再查 |

待辦：
- [x] 逐一下載並保存各字型的授權檔（OFL/LICENSE）到 `riddle-diary/licenses/`（見該資料夾 README）
- [x] **Tangerine 條款已確認**：為 SIL OFL 1.1（開頭「All rights reserved」是 OFL 版權人標準寫法，非矛盾）。本地打包合規。
- [ ] 確認「以 webfont 從 CDN 載入」是否符合各授權（OFL 允許嵌入/散佈，通常 OK；商用再三確認）
- [ ] 若要把字型檔打包進擴充（本地化方案），散佈即觸發授權條款，更需備齊授權檔
- [ ] 釐清本專案的散佈情境（自用 vs 上架 Chrome Web Store vs 開源）——散佈範圍不同，義務不同

---

## 📌 決策紀錄 Decision Log

> 更新：2026-06-19 12:47

### 架構
- **走 Chrome Extension 覆蓋 claude.ai**，不自接 API → 用使用者已登入的帳號，免 API key。
- 對話橋接：寫入 claude.ai 真正的輸入框 → 送出 → 輪詢讀回應。
- 所有 DOM 選擇器集中在 `content.js` 最上方的 `SELECTORS`，方便改版時維護。

### 字型載入策略 ⚠️ 已更正（2026-06-19 13:xx）
**真相**：claude.ai 的 CSP `font-src` 白名單為
`'self' assets.claude.ai assets-proxy.anthropic.com fonts.gstatic.com js.intercomcdn.com fonts.intercomcdn.com`
—— **不含 jsdelivr，也不含其他外部 CDN**。先前以為「jsdelivr 可用」是錯的：那段時間中英文其實都在用系統字 fallback 頂著。

最終做法（v0.3.1 起，已套用）：**兩款字型都本地打包，零對外請求。**
- **中文辰宇落雁體 + 英文 Tangerine**：字檔放在 `riddle-diary/fonts/`，由 `content.js` 用 `chrome.runtime.getURL()` 注入 `chrome-extension://` 的 `@font-face`（`Chenyu` / `Diary EN`）+ `web_accessible_resources`，繞過網站 CSP。**✅ 2026-06-19 實測通過**：`chrome-extension://` 字型不受 `font-src` 限制，封面與內文皆正確、Console 無紅字。
- 中途插曲：英文一度改用 gstatic（`fonts.gstatic.com` 在白名單內、可載），但為符合「純本機、零對外請求」的信任訴求，最後也改為本地打包。（注意：`@import fonts.googleapis.com` 那條路會被 `style-src` 擋，不可行。）
- 診斷指令（Console）：`[...document.fonts].map(f=>f.family+' '+f.status)`；或看 Console 的 `font-src ... blocked` 紅字。
- 歷史踩雷：曾把 `@fontsource` 版本釘成不存在的號碼 → 404；但那其實不是主因，主因是 jsdelivr 整個被 CSP 擋。

### 英文字型 → **Tangerine**（已套用，v0.3.1 起本地打包）
- 理由：優雅花體，最接近電影裡湯姆瑞斗的手寫；可讀性可接受。
- 備選：Pinyon Script（個人更喜歡，但長文較難讀）；IM Fell English（古印刷書，先前版本）。
- 實作：字檔 `fonts/Tangerine-700.ttf` 打包在擴充內，由 `content.js` 注入 `chrome-extension://` 的 `@font-face`（family `Diary EN`），加 `size-adjust: 175%` 把花體放大到與中文協調。**零對外請求**（先前曾用 gstatic，後改本地以符合「純本機」訴求）。

### 中文字型 → **辰宇落雁體 Chenyuluoyan**（已套用）
- 理由：繁體手寫行楷，全繁體覆蓋（jf 7000 字集），有手寫溫度，與 Tangerine 花體調性一致。
- 來源：jsdelivr gh — `https://cdn.jsdelivr.net/gh/Chenyu-otf/chenyuluoyan_thin@2.0/ChenYuluoyan-Thin.ttf`（4.5 MB ttf）。
- 實作：自訂 `@font-face`（family `Chenyu`）；缺字時退回霞鶩文楷（`LXGW WenKai TC` 仍保留在 fallback 鏈）。
- 曾比較落選：芫荽 Iansui、粉圓 Huninn、Klee One、思源宋體；書法風的 Yuji 三款（日系，繁體缺字）、馬善政（簡體，繁體缺字多）。

---

## ⚠️ 維護備忘 Maintenance notes

- claude.ai 改版時，先檢查 `content.js` 最上方的 `SELECTORS`（輸入框 / 送出鈕 / 助理訊息 / 停止鈕）。
- 新功能 2 會新增「對話列表」相關選擇器，一併集中在 `SELECTORS` 管理。
